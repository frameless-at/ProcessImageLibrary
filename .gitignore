# macOS / Dropbox sync droppings
.DS_Store
.dropbox*

# Stray AdminTheme files — never belong in this repo, but Dropbox
# sync has dumped them here at least once.
/AdminThemeUikit/
/AdminThemeReno/
/AdminThemeDefault/

# Third-party module kept locally only as a reference / study copy.
/MediaHub-1.19.0/

# ProcessWire core — Dropbox sync has dumped a full wire/ tree here; it is
# NOT part of this module repo. Kept on disk, never tracked.
/wire/
/processwire-*/
